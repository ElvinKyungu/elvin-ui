Debug and fix the issue in $ARGUMENTS.

- Identify the root cause (explain in one sentence)
- Apply the minimal fix — do not refactor unrelated code
- Keep the component structure intact
- Ensure SSR compatibility (no `window`/`document` outside `onMounted`)
- Return the corrected code + one-line explanation of the cause
